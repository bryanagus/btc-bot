name: Bot Trading BTC Pro WITA

on:
  #schedule:
    # Jalan otomatis setiap jam pada menit ke-10 (misal: 01:10, 02:10, 03:10)
    # - cron: '10 * * * *' 
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # MENGGUNAKAN KUNCI MASTER (TOKEN) UNTUK BYPASS PROTEKSI BRANCH
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.MY_TOKEN }} 
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          
      - name: Run Script
        run: python main.py
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

      # MENGGUNAKAN ACTION OTOMATIS DENGAN TOKEN UNTUK SIMPAN CSV & JSON (DITAMBAH PKL, ALERT STATE, DAN LOG SYSTEM)
      - name: Auto Save CSV & JSON to GitHub
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🤖 Auto-update Jurnal Prediksi AI [skip ci]"
          # Menambahkan bot_system.log agar riwayat kerja bot tersimpan di github
          file_pattern: 'ai_history_log.csv dashboard_data.json ai_model_1H.pkl ai_model_6H.pkl last_alert_state.json bot_system.log'
          #push_options: '--force' # Memaksa masuk jika branch terkunci

      # =========================================================
      # TAHAP 2: KIRIM DATA KE INFINITYFREE VIA FTP
      # =========================================================
      - name: 🚀 Upload ke Web InfinityFree
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: htdocs/ # Folder wajib InfinityFree untuk publik
          # Demi keamanan, JANGAN kirim source code python, CSV database, dan Log Sistem ke web publik!
          exclude: |
            **/.git*
            **/.git*/**
            main.py
            requirements.txt
            ai_history_log.csv
            README.md
            *.png
            *.pkl
            last_alert_state.json
            bot_system.log
